import 'package:flutter/material.dart';
import 'Pages/home_page.dart'; // ajusta la ruta si tu archivo está en otro folder

void main() {
  runApp(MaterialApp(home: HomePage(), debugShowCheckedModeBanner: false));
}